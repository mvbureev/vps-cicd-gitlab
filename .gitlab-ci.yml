image: docker:19.03

before_script:
  - apk add --no-cache docker-compose
  - docker info
  - docker-compose version
  - ls -lsa

stages:
  - deploy

deploy:
  stage: deploy
  script:
    - docker-compose ps
    - docker-compose up --build --force-recreate
  only:
    - master
