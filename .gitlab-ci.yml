image:
  name: docker:stable
  entrypoint: ["/bin/sh", "-c"]

services:
  - docker:dind

before_script:
  - apk add --no-cache docker-compose

stages:
  - deploy

deploy:
  stage: deploy
  script:
    - ls -lsa
    - sh ./bin/gitlab-deploy.sh
  only:
    - master
